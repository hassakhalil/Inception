version: '3.8'

services:
  wordpress:
    container_name: wordpress
    build: ./requirements/wordpress/
    depends_on:
      - mariadb
    volumes:
      - wp:/var/www/html
    networks:
      - mynetwork
    env_file:
      - var.env
  mariadb:
    container_name: mariadb
    build: ./requirements/mariadb/
    env_file:
      - var.env
    volumes:
      - db:/var/lib/mysql
    networks:
      - mynetwork
  nginx:
    container_name: nginx
    build: ./requirements/nginx/
    env_file:
      - var.env
    volumes:
      - wp:/var/www/html
    depends_on:
      - wordpress
    ports:
      - 443:443
    networks:
      - mynetwork
  adminer:
    container_name: adminer
    build: ./requirements/adminer/
    networks:
      - mynetwork
    depends_on:
      - nginx
      - mariadb
networks:
  mynetwork:

volumes:
  db:
    driver: local
    driver_opts:
      type: 'none'
      o: 'bind'
      device: '/Users/hassan/Desktop/Inception/srcs/home/hkhalil/db'
  wp:
    driver: local
    driver_opts:
      type: 'none'
      o: 'bind'
      device: '/Users/hassan/Desktop/Inception/srcs/home/hkhalil/wp'